sudo: required

services:
  - docker

language: node_js

deploy:
  - provider: script
    script: bash docker_push master $TRAVIS_TAG
    on:
      branch: master
  
  - provider: script
    script: bash docker_push edge
    on:
      branch: dev